<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.eatnote.model.dao.UserDao">

    <!-- 회원가입 -->
    <insert id="insertUser" parameterType="User">
        INSERT INTO users (
            email, password, name, nickname,
            gender, profile_image, user_type, status,
            created_at, updated_at
        )
        VALUES (
            #{email}, #{password}, #{name}, #{nickname},
            #{gender}, #{profileImage}, #{userType}, #{status},
            NOW(), NOW()
        )
    </insert>

    <!-- user_id(PK)로 사용자 조회 -->
    <select id="selectUserBySeq" parameterType="long" resultType="User">
        SELECT *
          FROM users
         WHERE user_id = #{userId}
    </select>

    <!-- email로 사용자 조회 -->
    <select id="selectUserByEmail" parameterType="string" resultType="User">
        SELECT *
          FROM users
         WHERE email = #{email}
    </select>

    <!-- 전체 사용자 목록 -->
    <select id="selectAllUsers" resultType="User">
        SELECT *
          FROM users
    </select>

    <!-- 회원 정보 수정 -->
    <update id="updateUser" parameterType="User">
        UPDATE users
        SET
            password = #{password},
            name = #{name},
            nickname = #{nickname},
            gender = #{gender},
            profile_image = #{profileImage},
            user_type = #{userType},
            status = #{status},
            updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <!-- 사용자 삭제 -->
    <delete id="deleteUserById" parameterType="long">
        DELETE FROM users
        WHERE user_id = #{userId}
    </delete>

</mapper>